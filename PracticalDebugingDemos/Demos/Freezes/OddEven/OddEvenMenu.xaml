<UserControl x:Class="PracticalDebuggingDemos.Demos.Freezes.OddEven.OddEvenMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Margin" Value="5"></Setter>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="5"></Setter>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="5"></Setter>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Margin" Value="5"></Setter>
        </Style>
    </UserControl.Resources>
    <StackPanel HorizontalAlignment="Center">
        <TextBlock>This will create a number of random numbers and then count Odds and Evens.</TextBlock>
        <TextBlock>Enter amount of numbers:</TextBlock>
        <TextBox Width="200" x:Name="numItems">1000</TextBox>
        <Button Click="StartCount">Start</Button>
        <TextBlock x:Name="resultTextBlock"></TextBlock>

    </StackPanel>
</UserControl>
